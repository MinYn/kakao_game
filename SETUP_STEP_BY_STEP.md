# 카카오 챗봇 관리자센터 단계별 설정 가이드

스크린샷을 참고하여 실제 화면에서 어떻게 설정하는지 단계별로 설명합니다.

**📖 관련 가이드**:
- 파라미터 설정: `PARAMETER_GUIDE.md`
- 발화 패턴 설정: `UTTERANCE_SETUP.md` ⭐
- 커맨드 형태 발화 패턴: `COMMAND_PATTERN_GUIDE.md` ⭐ (슬래시/@ 커맨드 등록)
- 문제 해결: `TROUBLESHOOTING.md` ⚠️ (폴백 블록 문제 해결)

---

## 1단계: 스킬 생성

### 1.1 스킬 메뉴 접속

1. 챗봇 관리자센터 접속: https://i.kakao.com/
2. 왼쪽 메뉴에서 **"스킬"** 클릭
3. **"+ 스킬 만들기"** 또는 **"스킬 만들기"** 버튼 클릭

### 1.2 스킬 정보 입력

**스킬 편집 화면에서**:

1. **스킬 이름**: `게임봇 스킬` 입력
2. **스킬 설명**: `게임봇의 모든 기능을 처리하는 통합 스킬` 입력 (선택사항)
3. **스킬 서버 URL**: 
   - 로컬 개발: `https://abc123.ngrok.io/webhook` (ngrok URL)
   - 서버 배포: `https://yourdomain.com/webhook`
4. **저장** 버튼 클릭

### 1.3 스킬 테스트

1. 스킬 상세 페이지에서 **"테스트"** 버튼 클릭
2. 테스트 메시지 입력: `골드`
3. 응답이 정상적으로 오는지 확인

---

## 2단계: 블록 생성 및 설정

### 2.1 블록 생성

1. 왼쪽 메뉴에서 **"시나리오"** 클릭
2. **"+ 블록 추가"** 버튼 클릭
3. 블록 이름 입력: `게임봇 메인 블록`
4. **저장** 또는 **확인** 클릭

### 2.2 사용자 발화 설정 (전체 발화 패턴 설정)

**"사용자 발화" 섹션에서**:

#### 방법 1: 모든 발화로 설정 (권장) ⭐

**목표**: 모든 메시지를 이 블록으로 연결

**단계별 설정**:

1. **"사용자 발화"** 섹션 확인
   - 화면 상단에 "사용자 발화" 제목과 물음표 아이콘 확인

2. **발화 패턴 입력 필드 확인**
   - "사용자가 입력할 것 같은 대표적인 발화를 입력해주세요" 플레이스홀더가 있는 입력 필드 확인

3. **"모든 발화" 옵션 활성화**
   - 방법 A: 발화 패턴 입력 필드를 **비워두고** 저장
   - 방법 B: 발화 패턴 목록 아래에 있는 **"모든 발화"** 체크박스 또는 토글 활성화
   - 방법 C: 발화 패턴을 모두 삭제하고 저장

4. **확인 방법**
   - 발화 패턴 목록이 비어있거나
   - "모든 발화" 옵션이 활성화되어 있으면 성공

**결과**: 
- 모든 사용자 메시지가 이 블록으로 연결됨
- 폴백 블록으로 가지 않고 스킬 서버로 전달됨

#### 방법 2: 발화 패턴 추가 (선택사항)

**목표**: 특정 발화 패턴만 이 블록으로 연결

**단계별 설정**:

1. **발화 패턴 입력**
   - 발화 패턴 입력 필드에 발화 입력 (예: `골드`)
   - **추가** 버튼 클릭 또는 **Enter** 키

2. **추가 발화 패턴 입력**
   - 필요한 발화 패턴들을 하나씩 추가:
     ```
     골드
     게임시작
     리더보드
     도움말
     게임종료
     ```
   - 각 발화 패턴은 별도로 추가

3. **"모든 발화" 옵션 비활성화**
   - 발화 패턴 목록 아래에 있는 "모든 발화" 옵션이 **비활성화**되어 있는지 확인

**결과**: 
- 입력한 발화 패턴과 일치하는 메시지만 이 블록으로 연결됨
- 일치하지 않는 메시지는 다른 블록(폴백 블록)으로 연결됨

#### 방법 3: 와일드카드 패턴 사용 (고급)

**목표**: 특정 패턴을 가진 모든 메시지 연결

**예시**:
- `게임시작 *` → "게임시작"으로 시작하는 모든 메시지
- `* 골드` → "골드"로 끝나는 모든 메시지

**주의**: 카카오 챗봇 관리자센터에서 와일드카드 지원 여부 확인 필요

---

### ⚠️ 중요 사항

**"모든 발화" vs "발화 패턴"**:

| 설정 방법 | 동작 | 권장 여부 |
|---------|------|---------|
| **모든 발화 활성화** | 모든 메시지가 이 블록으로 연결 | ✅ 권장 |
| **발화 패턴만 추가** | 패턴과 일치하는 메시지만 연결 | ❌ 비권장 (폴백 블록 문제 발생 가능) |
| **발화 패턴 + 모든 발화** | 모든 메시지가 이 블록으로 연결 (발화 패턴 무시) | ✅ 가능 |

**게임봇 권장 설정**:
- ✅ **"모든 발화" 활성화** (발화 패턴 없음)
- ✅ 발화 패턴 입력 필드 비워두기
- ✅ 발화 패턴 목록 비우기

**이유**:
- 모든 명령어를 스킬 서버에서 처리
- 폴백 블록 문제 없음
- 새로운 명령어 추가 시 시나리오 수정 불필요

### 2.3 파라미터 설정

**"파라미터 설정" 섹션에서**:

#### 방법 1: 파라미터 설정 안 함 (권장) ⭐

1. **일반 파라미터**: 설정 안 함
   - "일반 파라미터가 없습니다." 상태로 유지
2. **필수 파라미터**: 설정 안 함
   - "필수 파라미터가 없습니다." 상태로 유지

**장점**: 
- 설정이 간단함
- 스킬 서버에서 `userRequest.utterance`를 직접 파싱하여 처리 가능
- 유연한 명령어 처리

**스킬 서버에서 처리**:
```python
# webhook_server.py에서
data = await request.json()
utterance = data.get('userRequest', {}).get('utterance', '')
# utterance를 직접 파싱하여 처리 (예: "골드", "게임시작 숫자맞추기")
```

#### 방법 2: 파라미터 설정 (고급)

파라미터를 사용하려면:

**일반 파라미터 추가**:

1. **"일반 파라미터"** 박스에서 **"+"** 버튼 클릭
2. 파라미터 정보 입력:
   - **파라미터 이름**: `message` (또는 원하는 이름)
   - **파라미터 타입**: `String` 선택
   - **기본값**: `@{userRequest.utterance}` 입력
     - 이렇게 하면 사용자 발화 전체가 파라미터로 전달됨
3. **저장** 또는 **확인**

**필수 파라미터 추가** (선택사항):

1. **"필수 파라미터"** 박스에서 **"+"** 버튼 클릭
2. 파라미터 정보 입력:
   - **파라미터 이름**: `message`
   - **파라미터 타입**: `String`
   - **필수 여부**: 체크 (필수로 설정)
3. **저장**

**스킬 서버에서 파라미터 받기**:
```python
# webhook_server.py에서
data = await request.json()
action = data.get('action', {})
params = action.get('params', {})
message = params.get('message', '')

# 또는 utterance 직접 사용 (둘 다 가능)
utterance = data.get('userRequest', {}).get('utterance', '')
```

**파라미터 사용 예시**:

**요청 형식** (파라미터 사용 시):
```json
{
  "userRequest": {
    "user": {
      "id": "user123"
    },
    "utterance": "골드"
  },
  "action": {
    "params": {
      "message": "골드"
    }
  }
}
```

**주의사항**:
- 파라미터를 사용해도 `userRequest.utterance`는 항상 전달됨
- 파라미터 없이도 동작하므로 필수는 아님
- 게임봇의 경우 파라미터 없이 사용하는 것을 권장

### 2.4 응답 설정 (가장 중요!) ⭐

**응답 설정 섹션에서**:

1. **응답 타입 선택**:
   - **"스킬 연결"** 또는 **"스킬 사용"** 선택 ✅
   - ❌ "직접 응답" 선택하지 않기

2. **스킬 선택**:
   - 드롭다운에서 **"게임봇 스킬"** 선택
   - 앞서 생성한 스킬이 목록에 표시됨

3. **저장** 버튼 클릭

**⚠️ 중요**: 
- 응답 설정에서 스킬을 연결하지 않으면 폴백 블록으로 연결됨
- 반드시 "스킬 연결"을 선택하고 스킬을 지정해야 함

---

## 3단계: 블록 순서 확인

### 3.1 블록 순서 확인

**왼쪽 사이드바에서**:

1. 블록 목록 확인
2. **"게임봇 메인 블록"**이 **"폴백 블록"** 아래에 있는지 확인

**블록 순서 (권장)**:
```
기본 시나리오
├── 웰컴 블록 OFF
├── 폴백 블록          ← 맨 위 (시스템 블록)
└── 게임봇 메인 블록   ← 맨 아래 (우리가 만든 블록)
```

**⚠️ 중요**: 
- "게임봇 메인 블록"이 "폴백 블록" 위에 있으면 폴백 블록으로 연결될 수 있음
- "게임봇 메인 블록"을 **맨 아래**로 이동

### 3.2 블록 순서 변경 방법

1. 왼쪽 사이드바에서 블록을 드래그하여 순서 변경
2. 또는 블록 설정에서 순서 변경 옵션 사용

---

## 4단계: 폴백 블록 확인

### 4.1 폴백 블록 설정

**"폴백 블록" 설정 확인**:

1. 왼쪽 사이드바에서 **"폴백 블록"** 클릭
2. 폴백 블록 편집 화면 확인
3. **응답 설정** 확인:
   - 폴백 블록에도 스킬이 연결되어 있으면 안 됨
   - 또는 폴백 블록을 비활성화

**권장 설정**:
- 폴백 블록은 기본 메시지만 표시하도록 설정
- 또는 "게임봇 메인 블록"이 모든 발화를 처리하므로 폴백 블록 비활성화

---

## 5단계: 배포

### 5.1 시나리오 저장

1. 블록 편집 완료 후 **"저장"** 버튼 클릭
2. 변경사항이 저장되었는지 확인

### 5.2 챗봇 배포

1. 상단 메뉴에서 **"배포"** 버튼 클릭
2. 배포 확인 대화상자에서 **"배포"** 확인
3. 배포 완료 메시지 확인

**⚠️ 중요**: 
- 배포 후 몇 분 정도 기다려야 반영됨
- 배포 전에는 변경사항이 적용되지 않음

---

## 6단계: 테스트

### 6.1 카카오톡에서 테스트

1. 카카오톡 앱 실행
2. 채널 검색하여 추가
3. 메시지 전송:
   - `골드`
   - `게임시작 숫자맞추기`
   - `리더보드`
   - `도움말`

### 6.2 스킬 서버 로그 확인

**웹훅 서버 실행 중인 터미널에서**:

```bash
# 로그 확인
python main.py kakao
```

**정상 작동 시 로그 예시**:
```
[웹훅 요청] {'userRequest': {'user': {'id': '12345'}, 'utterance': '골드'}}
[웹훅 응답] 사용자: 12345, 메시지: 골드, 응답: 💰 현재 골드: 100G...
```

**문제가 있으면**:
- 로그가 없음 → 블록 설정 문제 (스킬 연결 확인)
- 오류 메시지 → 스킬 서버 코드 확인

---

## 문제 해결 체크리스트

### 폴백 블록으로 연결되는 경우

- [ ] 블록에 스킬이 연결되어 있는지 확인
- [ ] 응답 설정에서 "스킬 연결" 선택했는지 확인
- [ ] 스킬 이름이 정확한지 확인 ("게임봇 스킬")
- [ ] 블록 순서 확인 (게임봇 메인 블록이 맨 아래)
- [ ] 배포했는지 확인
- [ ] 스킬 서버가 실행 중인지 확인

### 스킬 서버가 호출되지 않는 경우

- [ ] 스킬 서버 URL이 정확한지 확인 (HTTPS 필수)
- [ ] ngrok이 실행 중인지 확인 (로컬 개발 시)
- [ ] 스킬 서버 로그에서 요청이 들어오는지 확인
- [ ] 카카오 챗봇 관리자센터 로그 확인

---

## 최종 확인 사항

### 블록 설정 확인

**"게임봇 메인 블록" 편집 화면에서**:

✅ **사용자 발화**: "모든 발화" 또는 발화 패턴 추가됨
✅ **파라미터 설정**: 설정 안 함 (또는 선택사항으로 설정)
✅ **응답 설정**: "스킬 연결" 선택됨
✅ **스킬 선택**: "게임봇 스킬" 선택됨
✅ **저장**: 저장 완료
✅ **배포**: 배포 완료

### 스킬 설정 확인

**"게임봇 스킬" 편집 화면에서**:

✅ **스킬 이름**: "게임봇 스킬"
✅ **스킬 서버 URL**: `https://yourdomain.com/webhook`
✅ **파라미터**: 설정 안 함 (권장)
✅ **응답 설정**: "스킬 서버 응답 사용" 선택
✅ **테스트**: 테스트 성공

---

## 빠른 참조

### 최소 설정 (가장 간단)

1. **스킬 1개**: `게임봇 스킬` 생성, URL 설정
2. **블록 1개**: `게임봇 메인 블록` 생성
   - 사용자 발화: **모든 발화** ✅
   - 파라미터: 설정 안 함 ✅
   - 응답: **스킬 연결** → `게임봇 스킬` 선택 ✅
3. **저장 및 배포** ✅

**완료!** 이제 모든 메시지가 스킬 서버로 전달됩니다.

---

## 추가 도움말

- **상세 가이드**: `SKILL_SCENARIO_GUIDE.md` 참조
- **발화 패턴 목록**: `SKILL_SCENARIO_GUIDE.md` 섹션 5 참조
- **문제 해결**: `SKILL_SCENARIO_GUIDE.md` 섹션 12 참조

