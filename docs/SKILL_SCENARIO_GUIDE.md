# 스킬 및 시나리오 등록 가이드

카카오 챗봇 관리자센터에서 게임봇을 위한 스킬과 시나리오를 등록하는 방법을 설명합니다.

**📸 실제 화면 기준 단계별 가이드는 `SETUP_STEP_BY_STEP.md`를 참조하세요!**

---

## 📋 목차

1. [스킬 등록](#1-스킬-등록)
2. [스킬 파라미터 설정](#2-스킬-파라미터-설정)
3. [스킬 응답 설정](#3-스킬-응답-설정)
4. [시나리오 등록](#4-시나리오-등록)
5. [발화 패턴 목록](#5-발화-패턴-목록)
6. [블록 구성 예시](#6-블록-구성-예시)
7. [폴백 블록 문제 해결](#폴백-블록으로-연결되는-문제-해결) ⚠️

---

## 1. 스킬 등록

### 1.1 스킬 생성

**위치**: 챗봇 관리자센터 → 스킬 → 스킬 만들기

**스킬 정보**:
- **스킬 이름**: `게임봇 스킬` (또는 원하는 이름)
- **스킬 설명**: `게임봇의 모든 기능을 처리하는 통합 스킬`
- **스킬 서버 URL**: `https://yourdomain.com/webhook` (또는 ngrok URL)

**중요**: 스킬은 **하나만** 생성하면 됩니다. 모든 명령어를 하나의 스킬 서버에서 처리합니다.

### 1.2 스킬 파라미터 설정 (선택사항)

**위치**: 스킬 상세 페이지 → 파라미터 설정

**파라미터란?**
- 스킬 서버로 전달할 추가 정보를 정의하는 것
- 엔티티에서 추출한 값을 파라미터로 전달 가능

**게임봇의 경우**:
- **파라미터 설정 불필요** (권장)
- 스킬 서버에서 `userRequest.utterance`를 직접 파싱하여 처리
- 모든 명령어를 하나의 엔드포인트에서 처리

**파라미터를 사용하는 경우** (고급):

만약 파라미터를 사용하고 싶다면:

1. **파라미터 추가**
   - 파라미터 이름: `message`
   - 파라미터 타입: `String`
   - 기본값: `@{userRequest.utterance}` (사용자 발화 전체)

2. **스킬 서버에서 파라미터 받기**
   ```json
   {
     "action": {
       "params": {
         "message": "사용자가 입력한 메시지"
       }
     }
   }
   ```

**권장 설정**: 파라미터 없이 사용 (간단하고 유연함)

---

## 2. 스킬 응답 설정

### 2.1 응답 타입 설정

**위치**: 스킬 상세 페이지 → 응답 설정

**응답 타입 선택**:
- **스킬 서버 응답 사용** (권장) ✅
  - 스킬 서버에서 반환하는 JSON 응답을 그대로 사용
  - 가장 유연하고 강력한 방법

### 2.2 스킬 서버 응답 형식

스킬 서버는 다음 형식으로 응답해야 합니다:

**기본 응답 형식**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "응답 메시지",
          "extra": {}
        }
      }
    ],
    "quickReplies": []
  }
}
```

**응답 필드 설명**:
- `version`: API 버전 (항상 "2.0")
- `template.outputs`: 응답 메시지 배열
- `simpleText.text`: 실제 표시될 텍스트
- `simpleText.extra`: 추가 정보 (빈 객체 `{}`로 설정, 선택사항)
- `template.quickReplies`: 빠른 응답 버튼 배열 (빈 배열 `[]`로 설정, 선택사항)

**참고**: `extra`와 `quickReplies`는 선택사항이지만, 카카오 챗봇 관리자센터의 표준 형식에 맞추기 위해 포함하는 것을 권장합니다.

### 2.3 다양한 응답 타입 (참고)

스킬 서버에서 다양한 응답 타입을 사용할 수 있습니다:

**1. 간단한 텍스트** (현재 사용 중):
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "💰 현재 골드: 100G",
          "extra": {}
        }
      }
    ],
    "quickReplies": []
  }
}
```

**2. 이미지 포함**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleImage": {
          "imageUrl": "https://example.com/image.jpg",
          "altText": "이미지 설명"
        }
      }
    ],
    "quickReplies": []
  }
}
```

**3. 카드형 응답**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "basicCard": {
          "title": "게임 목록",
          "description": "사용 가능한 게임",
          "buttons": [
            {
              "action": "message",
              "label": "숫자맞추기",
              "messageText": "게임시작 숫자맞추기"
            }
          ]
        }
      }
    ],
    "quickReplies": []
  }
}
```

**현재 게임봇**: `simpleText` 형식만 사용 (텍스트 기반)

### 2.4 응답 설정 체크리스트

- [ ] 응답 타입: "스킬 서버 응답 사용" 선택
- [ ] 스킬 서버가 올바른 JSON 형식으로 응답하는지 확인
- [ ] 테스트 메시지로 응답 확인

---

## 3. 블록 응답 설정

### 3.1 블록에서 응답 설정

**위치**: 블록 편집 → 응답 설정

**응답 설정 옵션**:

1. **스킬 연결 사용** (권장) ✅
   - 스킬 서버의 응답을 그대로 사용
   - 블록에 스킬을 연결하면 자동으로 적용됨

2. **직접 응답 설정** (선택사항)
   - 스킬 응답 전/후에 추가 메시지 표시 가능
   - 예: "처리 중입니다..." → 스킬 응답 → "감사합니다!"

**게임봇 권장 설정**:
- 스킬 연결만 사용
- 추가 응답 설정 불필요 (스킬 서버에서 모든 응답 처리)

---

## 4. 시나리오 등록

시나리오는 사용자의 발화 패턴에 따라 적절한 블록으로 라우팅합니다.

### 2.1 시나리오 구조

```
시나리오 (게임봇)
├── 블록: 골드 조회
├── 블록: 골드 전송
├── 블록: 리더보드
├── 블록: 게임 목록
├── 블록: 도움말
├── 블록: 게임 시작
│   ├── 숫자맞추기
│   ├── 가위바위보
│   └── 모험
├── 블록: 게임 종료
└── 블록: 기본 응답 (모든 발화)
```

---

## 5. 발화 패턴 목록

### 3.1 골드 조회 블록

**블록 이름**: `골드 조회`

**발화 패턴**:
```
골드
gold
g
포인트
point
points
잔액
내골드
p
pt
```

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.2 골드 전송 블록

**블록 이름**: `골드 전송`

**발화 패턴**:
```
골드주기 {사용자} {금액}
골드전송 {사용자} {금액}
pay {사용자} {금액}
send {사용자} {금액}
```

**엔티티 설정** (선택사항):
- `{사용자}`: 사용자 이름 (텍스트)
- `{금액}`: 숫자

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.3 리더보드 블록

**블록 이름**: `리더보드`

**발화 패턴**:
```
리더보드
랭킹
leaderboard
ranking
l
lb
rank
r
```

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.4 게임 목록 블록

**블록 이름**: `게임 목록`

**발화 패턴**:
```
게임목록
게임
games
list
gl
```

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.5 도움말 블록

**블록 이름**: `도움말`

**발화 패턴**:
```
도움말
help
?
h
```

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.6 게임 시작 블록

**블록 이름**: `게임 시작`

**발화 패턴**:
```
게임시작 {게임이름}
시작 {게임이름}
s {게임이름}
start {게임이름}
gs {게임이름}
```

**게임 이름 예시**:
- 숫자맞추기, number, n, 1
- 가위바위보, rps, r, 2
- 모험, adventure, a, adv, 3

**엔티티 설정** (선택사항):
- `{게임이름}`: 게임 이름 (텍스트)

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.7 게임 종료 블록

**블록 이름**: `게임 종료`

**발화 패턴**:
```
게임종료
종료
end
e
ge
```

**스킬 연결**: `게임봇 스킬` 선택

---

### 3.8 기본 응답 블록 (모든 발화)

**블록 이름**: `기본 응답`

**발화 패턴**: 
- **모든 발화** 또는 **기본 블록**으로 설정

**스킬 연결**: `게임봇 스킬` 선택

**설명**: 이 블록은 다른 블록과 매칭되지 않는 모든 발화를 처리합니다. 게임 중 명령어도 여기서 처리됩니다.

---

## 6. 블록 구성 예시

### 4.1 간단한 구성 (권장)

**방법**: 모든 발화를 하나의 블록으로 처리

1. **블록 생성**: `게임봇 메인 블록`
2. **발화 패턴**: **모든 발화** (기본 블록) ⚠️ 중요: 기본 블록으로 설정
3. **스킬 연결**: `게임봇 스킬`
4. **응답 설정**: "스킬 연결 사용" 선택
5. **블록 순서**: 기본 블록은 **맨 아래**에 위치 (다른 블록이 있으면)

**⚠️ 중요**: 
- 기본 블록(모든 발화)이 **맨 위**에 있으면 다른 블록과 매칭되지 않음
- 기본 블록은 **맨 아래**에 배치해야 함
- 또는 기본 블록만 사용하는 것을 권장

**설명**: 스킬 서버에서 모든 명령어를 파싱하여 처리

**장점**:
- 설정이 간단함
- 유지보수가 쉬움
- 새로운 명령어 추가 시 시나리오 수정 불필요
- 폴백 블록 문제 없음

---

### 4.2 상세 구성 (선택사항)

**방법**: 각 기능별로 블록 분리

1. **골드 조회 블록** → 골드 관련 발화 패턴
2. **게임 시작 블록** → 게임 시작 관련 발화 패턴
3. **기본 응답 블록** → 나머지 모든 발화

**장점**:
- 발화 패턴 관리가 명확함
- 각 기능별 통계 확인 가능

**단점**:
- 설정이 복잡함
- 새로운 명령어 추가 시 여러 블록 수정 필요

---

## 7. 등록 체크리스트

### 스킬 등록
- [ ] 스킬 생성 완료
- [ ] 스킬 서버 URL 설정 완료
- [ ] 스킬 파라미터 설정 (선택사항, 권장: 설정 안 함)
- [ ] 스킬 응답 설정: "스킬 서버 응답 사용" 선택
- [ ] 스킬 테스트 성공

### 시나리오 등록
- [ ] 기본 응답 블록 생성 (모든 발화)
- [ ] 기본 응답 블록에 스킬 연결
- [ ] 블록 응답 설정: "스킬 연결 사용" 선택
- [ ] (선택) 각 기능별 블록 생성
- [ ] (선택) 각 블록에 발화 패턴 등록
- [ ] (선택) 각 블록에 스킬 연결

### 테스트
- [ ] 골드 조회 테스트
- [ ] 게임 시작 테스트
- [ ] 게임 플레이 테스트
- [ ] 리더보드 테스트
- [ ] 도움말 테스트

---

## 8. 빠른 시작 (최소 설정)

### 최소 설정으로 시작하기

1. **스킬 1개 생성**
   - 이름: `게임봇 스킬`
   - URL: `https://yourdomain.com/webhook`
   - **파라미터 설정**: 설정 안 함 (권장)
   - **응답 설정**: "스킬 서버 응답 사용" 선택

2. **블록 1개 생성** (⚠️ 중요: 기본 블록만 사용)
   - 이름: `게임봇 메인 블록`
   - 발화 패턴: **모든 발화** (기본 블록) ⚠️
   - 스킬 연결: `게임봇 스킬`
   - 응답 설정: "스킬 연결 사용" 선택
   - **블록 순서**: 기본 블록은 맨 아래 (다른 블록이 있으면)

3. **기존 블록 확인** (중요!)
   - 다른 블록이 있다면 모두 삭제하거나 비활성화
   - 기본 블록만 활성화
   - 기본 블록이 맨 아래에 있는지 확인

4. **배포**
   - 시나리오 저장
   - 챗봇 배포

**완료!** 이제 모든 명령어가 스킬 서버로 전달되어 처리됩니다.

**⚠️ 폴백 블록 문제가 발생하면**:
- 모든 블록 삭제 후 기본 블록만 다시 생성
- 기본 블록에 스킬이 연결되어 있는지 확인
- 블록 순서 확인 (기본 블록이 맨 아래)

---

## 9. 주의사항

### 발화 패턴 우선순위

카카오 챗봇 관리자센터는 발화 패턴을 **위에서 아래 순서**로 매칭합니다.

**권장 순서**:
1. 구체적인 발화 패턴 (예: "게임시작 숫자맞추기")
2. 일반적인 발화 패턴 (예: "게임시작 {게임이름}")
3. 기본 블록 (모든 발화)

### 폴백 블록으로 연결되는 문제 해결

**문제**: 모든 발화가 폴백 블록으로 연결됨

**원인 및 해결 방법**:

1. **기본 블록이 모든 발화를 잡아먹는 경우**
   - **원인**: 기본 블록이 맨 위에 있거나, 다른 블록보다 우선순위가 높음
   - **해결**: 기본 블록을 **맨 아래**로 이동
   - **방법**: 시나리오 편집에서 블록 순서 변경

2. **발화 패턴이 정확히 일치하지 않는 경우**
   - **원인**: 공백, 대소문자, 특수문자 차이
   - **해결**: 발화 패턴을 정확히 입력 (예: "골드" ≠ " 골드 " ≠ "골드!")
   - **방법**: 발화 패턴에서 공백 제거, 대소문자 확인

3. **스킬이 연결되지 않은 경우**
   - **원인**: 블록에 스킬이 연결되지 않음
   - **해결**: 블록 편집 → 응답 설정 → 스킬 연결 확인
   - **방법**: "게임봇 스킬"이 선택되어 있는지 확인

4. **엔티티 설정 문제**
   - **원인**: 엔티티가 필수로 설정되어 있지만 값이 없음
   - **해결**: 엔티티를 선택사항으로 설정하거나 제거
   - **방법**: 발화 패턴에서 `{게임이름}` 같은 엔티티를 선택사항으로 설정

5. **블록이 비활성화된 경우**
   - **원인**: 블록이 활성화되지 않음
   - **해결**: 블록 상태 확인 및 활성화
   - **방법**: 시나리오 편집에서 블록 활성화 확인

**권장 해결 방법** (가장 간단):

1. **모든 블록 삭제**
2. **기본 블록 1개만 생성**
   - 블록 이름: `게임봇 메인 블록`
   - 발화 패턴: **모든 발화** (기본 블록)
   - 스킬 연결: `게임봇 스킬`
   - 응답 설정: "스킬 연결 사용"
3. **배포**

이렇게 하면 모든 발화가 스킬 서버로 전달되어 처리됩니다.

### 엔티티 사용

엔티티를 사용하면 발화 패턴을 더 유연하게 만들 수 있습니다:

```
게임시작 {게임이름}
골드주기 {사용자} {금액}
```

하지만 엔티티 없이도 스킬 서버에서 파싱 가능하므로 필수는 아닙니다.

### 게임 중 명령어

게임 중에는 게임별 명령어가 필요합니다:
- **숫자맞추기**: 숫자 입력 (1~100)
- **가위바위보**: "가위", "바위", "보"
- **모험**: "강화", "사냥", "상태" 등

이러한 명령어는 **기본 응답 블록**에서 처리됩니다.

---

## 10. 파라미터 및 응답 설정 상세 가이드

### 10.1 파라미터 설정 상세

#### 파라미터를 사용하지 않는 경우 (권장)

**장점**:
- 설정이 간단함
- 스킬 서버에서 모든 파싱 처리 가능
- 유연한 명령어 처리

**스킬 서버에서 처리**:
```python
# webhook_server.py에서
data = await request.json()
utterance = data.get('userRequest', {}).get('utterance', '')
# utterance를 직접 파싱하여 처리
```

#### 파라미터를 사용하는 경우

**파라미터 추가 방법**:

1. **스킬 편집** → **파라미터** 탭
2. **파라미터 추가** 클릭
3. 파라미터 정보 입력:
   - **파라미터 이름**: `message`
   - **파라미터 타입**: `String`
   - **필수 여부**: 선택 (권장: 선택)
   - **기본값**: `@{userRequest.utterance}`

**스킬 서버에서 파라미터 받기**:
```python
# webhook_server.py에서
data = await request.json()
action = data.get('action', {})
params = action.get('params', {})
message = params.get('message', '')
# 또는 utterance 직접 사용
utterance = data.get('userRequest', {}).get('utterance', '')
```

**주의사항**:
- 파라미터를 사용해도 `userRequest.utterance`는 항상 전달됨
- 파라미터 없이도 동작하므로 필수는 아님

### 10.2 응답 설정 상세

#### 스킬 서버 응답 형식

**필수 필드**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "응답 메시지"
        }
      }
    ]
  }
}
```

**응답 예시**:

1. **골드 조회 응답**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "💰 현재 골드: 100G",
          "extra": {}
        }
      }
    ],
    "quickReplies": []
  }
}
```

2. **게임 시작 응답**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "🎮 숫자 맞추기 게임 시작! (입장료: 10G)\n1부터 100까지의 숫자를 맞춰보세요.\n최대 10번의 기회가 있습니다.",
          "extra": {}
        }
      }
    ],
    "quickReplies": []
  }
}
```

3. **리더보드 응답**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "🏆 골드 리더보드\n\n🥇 user1: 500G\n🥈 user2: 300G\n🥉 user3: 200G",
          "extra": {}
        }
      }
    ],
    "quickReplies": []
  }
}
```

#### 응답 오류 처리

**오류 응답 형식**:
```json
{
  "version": "2.0",
  "template": {
    "outputs": [
      {
        "simpleText": {
          "text": "❌ 오류가 발생했습니다: {오류 메시지}",
          "extra": {}
        }
      }
    ],
    "quickReplies": []
  }
}
```

**HTTP 상태 코드**:
- `200`: 정상 응답
- `400`: 잘못된 요청
- `500`: 서버 오류

### 10.3 블록 응답 설정

#### 스킬 연결 사용 (권장)

**설정 방법**:
1. 블록 편집 → **응답** 탭
2. **스킬 연결** 선택
3. 앞서 만든 스킬 선택 (예: "게임봇 스킬")
4. 저장

**동작 방식**:
- 사용자 발화 → 블록 매칭 → 스킬 서버 호출 → 스킬 응답 표시

#### 추가 응답 설정 (선택사항)

**스킬 응답 전 메시지**:
- 예: "처리 중입니다..."
- 스킬 서버 호출 전에 표시

**스킬 응답 후 메시지**:
- 예: "감사합니다!"
- 스킬 서버 응답 후에 표시

**게임봇 권장**: 추가 응답 설정 불필요

---

## 11. 참고 자료

- **챗봇 관리자센터**: https://i.kakao.com/
- **스킬 개발 가이드**: https://kakaobusiness.gitbook.io/main/tool/chatbot/skill_guide
- **시나리오 가이드**: https://kakaobusiness.gitbook.io/main/tool/chatbot/scenario_guide
- **발화 패턴 가이드**: https://kakaobusiness.gitbook.io/main/tool/chatbot/utterance_pattern

---

## 12. 문제 해결

### 폴백 블록으로 연결되는 문제 ⚠️

**증상**: 모든 발화가 폴백 블록으로 연결되어 스킬 서버가 호출되지 않음

**해결 방법**:

#### 방법 1: 기본 블록 순서 확인 (가장 흔한 원인)

1. **시나리오 편집** 접속
2. **블록 순서 확인**
   - 기본 블록(모든 발화)이 **맨 아래**에 있어야 함
   - 기본 블록이 위에 있으면 모든 발화를 먼저 잡아먹음
3. **블록 순서 변경**
   - 기본 블록을 드래그하여 맨 아래로 이동
   - 또는 다른 블록들을 위로 이동

#### 방법 2: 발화 패턴 정확성 확인

1. **발화 패턴 확인**
   - 공백 없이 정확히 입력했는지 확인
   - 예: "골드" (O), " 골드 " (X), "골드!" (X)
2. **대소문자 확인**
   - 발화 패턴은 대소문자를 구분하지 않지만, 정확히 입력 권장
3. **특수문자 확인**
   - 불필요한 특수문자 제거

#### 방법 3: 스킬 연결 확인

1. **블록 편집** → **응답** 탭
2. **스킬 연결** 확인
   - "스킬 연결 사용" 선택되어 있는지 확인
   - "게임봇 스킬"이 선택되어 있는지 확인
3. **저장** 후 **배포**

#### 방법 4: 기본 블록만 사용 (가장 확실한 방법)

1. **모든 블록 삭제** (또는 비활성화)
2. **기본 블록 1개만 생성**
   - 발화 패턴: **모든 발화** (기본 블록)
   - 스킬 연결: `게임봇 스킬`
   - 응답 설정: "스킬 연결 사용"
3. **배포**

이렇게 하면 모든 발화가 스킬 서버로 전달됩니다.

#### 방법 5: 스킬 서버 로그 확인

1. **스킬 서버 로그 확인**
   ```bash
   # 웹훅 서버 실행 시 로그 확인
   python main.py kakao
   ```
2. **요청이 들어오는지 확인**
   - 로그에 `[웹훅 요청]`이 보이면 스킬 서버는 정상 작동
   - 로그가 없으면 블록 설정 문제

#### 방법 6: 카카오 챗봇 관리자센터 로그 확인

1. **챗봇 관리자센터** → **대시보드** → **로그**
2. **요청 로그 확인**
   - 스킬 서버로 요청이 전송되었는지 확인
   - 오류 메시지 확인

### 스킬이 호출되지 않는 경우

1. 스킬 서버 URL 확인 (HTTPS 필수)
2. 스킬 서버가 실행 중인지 확인
3. 블록에 스킬이 연결되어 있는지 확인
4. 발화 패턴이 올바르게 등록되었는지 확인
5. 블록 순서 확인 (기본 블록이 맨 아래인지)

### 응답이 오지 않는 경우

1. 스킬 서버 로그 확인
2. 응답 형식 확인 (JSON 형식, `extra`, `quickReplies` 포함)
3. 카카오 챗봇 관리자센터 로그 확인
4. HTTP 상태 코드 확인 (200이어야 함)

### 게임 명령어가 작동하지 않는 경우

1. 기본 응답 블록이 모든 발화를 처리하도록 설정
2. 스킬 서버에서 게임 상태 확인 로직 확인
3. 게임 중 명령어는 기본 블록에서 처리되어야 함

---

**완료!** 이제 카카오 챗봇 관리자센터에서 게임봇을 사용할 준비가 되었습니다! 🎉

