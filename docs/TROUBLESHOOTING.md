# 문제 해결 가이드

카카오 챗봇 관리자센터에서 발생하는 일반적인 문제와 해결 방법을 정리합니다.

---

## 문제 1: "게임시작 모험" 입력 시 폴백 블록으로 연결됨

### 증상

사용자가 "게임시작 모험"을 입력해도:
- 폴백 블록의 기본 메시지만 표시됨
- 스킬 서버가 호출되지 않음
- 게임이 시작되지 않음

### 원인 분석

**가능한 원인들**:

1. **발화 패턴이 "모든 발화"로 설정되지 않음** ⚠️ 가장 흔함
2. **블록 순서 문제** (폴백 블록이 먼저 매칭됨)
3. **스킬이 연결되지 않음**
4. **배포되지 않음**
5. **스킬 서버가 실행되지 않음**

### 해결 방법

#### 방법 1: 발화 패턴 확인 및 수정 (가장 중요)

**단계별 해결**:

1. **"게임봇 메인 블록" 편집**
   - 시나리오 → "게임봇 메인 블록" 클릭

2. **"사용자 발화" 섹션 확인**
   - 발화 패턴 목록 확인
   - 발화 패턴이 있다면 모두 삭제

3. **"모든 발화" 활성화**
   - 발화 패턴 입력 필드 비워두기
   - "모든 발화" 옵션 활성화 (있는 경우)
   - 또는 발화 패턴을 모두 삭제하고 저장

4. **저장**

**확인**:
- ✅ 발화 패턴 목록이 비어있음
- ✅ "모든 발화" 옵션이 활성화되어 있음 (있는 경우)

#### 방법 2: 블록 순서 확인

**단계별 해결**:

1. **시나리오 편집** 화면에서
2. **왼쪽 사이드바** 확인
3. **블록 순서** 확인:
   ```
   기본 시나리오
   ├── 웰컴 블록 OFF
   ├── 폴백 블록          ← 맨 위
   └── 게임봇 메인 블록   ← 맨 아래 (이 위치여야 함)
   ```

4. **순서 변경** (필요한 경우):
   - "게임봇 메인 블록"을 드래그하여 맨 아래로 이동
   - 또는 다른 블록들을 위로 이동

**확인**:
- ✅ "게임봇 메인 블록"이 "폴백 블록" 아래에 있음

#### 방법 3: 스킬 연결 확인

**단계별 해결**:

1. **"게임봇 메인 블록" 편집**
2. **"응답 설정"** 탭 클릭
3. **응답 타입 확인**:
   - ✅ "스킬 연결" 또는 "스킬 사용" 선택되어 있어야 함
   - ❌ "직접 응답" 선택되어 있으면 안 됨

4. **스킬 선택 확인**:
   - ✅ "게임봇 스킬"이 선택되어 있어야 함
   - ❌ 스킬이 선택되지 않았으면 안 됨

5. **저장**

**확인**:
- ✅ 응답 설정: "스킬 연결" 선택됨
- ✅ 스킬: "게임봇 스킬" 선택됨

#### 방법 4: 배포 확인

**단계별 해결**:

1. **변경사항 저장**
   - 블록 편집 후 "저장" 버튼 클릭

2. **챗봇 배포**
   - 상단 메뉴에서 "배포" 버튼 클릭
   - 배포 확인 대화상자에서 "배포" 확인

3. **대기**
   - 배포 후 몇 분 정도 기다림
   - 변경사항이 반영되는데 시간이 걸릴 수 있음

**확인**:
- ✅ 저장 완료
- ✅ 배포 완료
- ✅ 배포 후 몇 분 대기

#### 방법 5: 스킬 서버 확인

**단계별 해결**:

1. **스킬 서버 실행 확인**
   ```bash
   python main.py kakao
   ```
   - 서버가 실행 중이어야 함
   - 로그에 "웹훅 서버 시작" 메시지 확인

2. **스킬 서버 URL 확인**
   - 스킬 설정에서 URL이 정확한지 확인
   - HTTPS 필수 (HTTP는 사용 불가)
   - `/webhook` 경로 포함 확인

3. **스킬 서버 로그 확인**
   - 메시지 전송 시 로그에 `[웹훅 요청]`이 나타나는지 확인
   - 로그가 없으면 블록 설정 문제

**확인**:
- ✅ 스킬 서버 실행 중
- ✅ 스킬 서버 URL 정확함 (HTTPS)
- ✅ 로그에 요청이 들어옴

---

## 즉시 해결 방법 (가장 확실)

### 완전 초기화 후 재설정

1. **모든 블록 삭제**
   - 시나리오 편집에서 모든 블록 삭제
   - 또는 기존 블록 비활성화

2. **"게임봇 메인 블록" 새로 생성**
   - 블록 이름: `게임봇 메인 블록`
   - 사용자 발화: **비워두기** (모든 발화)
   - 파라미터 설정: 설정 안 함
   - 응답 설정: **스킬 연결** → "게임봇 스킬" 선택

3. **블록 순서 확인**
   - "게임봇 메인 블록"이 맨 아래에 있는지 확인

4. **저장 및 배포**
   - 저장 → 배포 → 대기

5. **테스트**
   - "게임시작 모험" 입력
   - 스킬 서버 로그 확인

---

## 문제 2: 특정 명령어만 폴백 블록으로 연결됨

### 증상

- "골드" 입력 → 정상 작동
- "게임시작 모험" 입력 → 폴백 블록으로 연결

### 원인

발화 패턴에 특정 명령어만 등록되어 있고, "모든 발화"가 비활성화되어 있음

### 해결

1. 발화 패턴에 "게임시작 모험" 추가
2. 또는 발화 패턴을 모두 삭제하고 "모든 발화" 활성화 (권장)

---

## 문제 3: 스킬 서버는 호출되지만 응답이 없음

### 증상

- 스킬 서버 로그에 요청이 들어옴
- 하지만 사용자에게 응답이 전달되지 않음

### 원인

- 응답 형식이 잘못됨
- HTTP 상태 코드가 200이 아님

### 해결

1. **응답 형식 확인**
   ```json
   {
     "version": "2.0",
     "template": {
       "outputs": [
         {
           "simpleText": {
             "text": "응답 메시지",
             "extra": {}
           }
         }
       ],
       "quickReplies": []
     }
   }
   ```

2. **HTTP 상태 코드 확인**
   - 정상 응답: 200
   - 오류 응답: 400, 500 등

3. **스킬 서버 로그 확인**
   - 오류 메시지 확인
   - 응답 형식 확인

---

## 문제 4: 게임이 시작되지 않음

### 증상

- "게임시작 모험" 입력
- 스킬 서버는 호출됨
- 하지만 게임이 시작되지 않음

### 원인

- 스킬 서버 코드 문제
- 게임 엔진 로직 문제

### 해결

1. **스킬 서버 로그 확인**
   ```bash
   python main.py kakao
   ```
   - 오류 메시지 확인
   - 응답 메시지 확인

2. **게임 엔진 코드 확인**
   - `game_engine.py`의 `_start_game` 메서드 확인
   - `games/adventure.py`의 `start` 메서드 확인

3. **CLI 모드에서 테스트**
   ```bash
   python cli.py
   ```
   - "게임시작 모험" 입력
   - 정상 작동하는지 확인

---

## 체크리스트

### 폴백 블록 문제 해결 체크리스트

- [ ] 발화 패턴이 "모든 발화"로 설정되어 있음
- [ ] 발화 패턴 목록이 비어있음
- [ ] 블록 순서 확인 (게임봇 메인 블록이 맨 아래)
- [ ] 응답 설정에서 "스킬 연결" 선택됨
- [ ] 스킬 "게임봇 스킬" 선택됨
- [ ] 저장 완료
- [ ] 배포 완료
- [ ] 스킬 서버 실행 중
- [ ] 스킬 서버 URL 정확함 (HTTPS)
- [ ] 스킬 서버 로그에서 요청 확인됨

---

## 빠른 진단

### 1단계: 스킬 서버 로그 확인

```bash
python main.py kakao
```

**메시지 전송 후**:
- ✅ 로그에 `[웹훅 요청]` 나타남 → 블록 설정 문제
- ❌ 로그에 아무것도 없음 → 블록 설정 문제 (스킬 연결 안 됨)

### 2단계: 블록 설정 확인

**"게임봇 메인 블록" 편집 화면에서**:
- ✅ 사용자 발화: 비어있음 또는 "모든 발화" 활성화
- ✅ 응답 설정: "스킬 연결" 선택됨
- ✅ 스킬: "게임봇 스킬" 선택됨

### 3단계: 배포 확인

- ✅ 저장 완료
- ✅ 배포 완료
- ✅ 배포 후 몇 분 대기

---

## 요약

**"게임시작 모험"이 폴백 블록으로 연결되는 경우**:

1. **발화 패턴 확인** → "모든 발화" 활성화
2. **블록 순서 확인** → 게임봇 메인 블록이 맨 아래
3. **스킬 연결 확인** → 응답 설정에서 스킬 연결
4. **배포 확인** → 저장 후 배포
5. **스킬 서버 확인** → 실행 중이고 로그 확인

**가장 확실한 해결**: 모든 블록 삭제 후 기본 블록만 생성하고 "모든 발화" 활성화

