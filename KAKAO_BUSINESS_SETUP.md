# 카카오 비즈니스 채널 웹훅 설정 가이드

이 가이드는 카카오 비즈니스 채널에서 웹훅을 설정하여 완전 무료로 챗봇을 사용하는 방법을 단계별로 설명합니다.

## 사전 준비

1. **카카오 계정** (카카오톡 사용 계정)
2. **웹훅 서버 실행** (`python main.py kakao --webhook`)
3. **ngrok 또는 공개 서버 URL** (웹훅 URL 필요)

---

## 단계별 설정 가이드

### 1단계: 카카오 비즈니스 접속 및 로그인

1. **카카오 비즈니스 사이트 접속**
   - 브라우저에서 https://business.kakao.com/ 접속
   - 우측 상단 **"로그인"** 버튼 클릭

   📸 **스크린샷 위치**: 메인 페이지 상단의 로그인 버튼

2. **카카오 계정으로 로그인**
   - 카카오 계정으로 로그인
   - 처음 접속하는 경우 약관 동의 필요

   📸 **스크린샷 위치**: 로그인 페이지

---

### 2단계: 채널 생성 (처음 사용하는 경우)

1. **채널 생성**
   - 로그인 후 대시보드에서 **"채널 만들기"** 또는 **"채널 관리"** 클릭
   - **"카카오톡 채널 만들기"** 선택

   📸 **스크린샷 위치**: 대시보드의 "채널 만들기" 버튼

2. **채널 정보 입력**
   - **채널 이름**: 원하는 이름 입력 (예: "게임봇")
   - **채널 프로필**: 프로필 이미지, 소개글 등 설정
   - **카테고리**: 적절한 카테고리 선택
   - **저장** 클릭

   📸 **스크린샷 위치**: 채널 생성 폼

---

### 3단계: 챗봇 설정

1. **채널 관리 페이지 접속**
   - 생성한 채널 선택
   - 왼쪽 메뉴에서 **"챗봇"** 또는 **"자동응답"** 메뉴 클릭

   📸 **스크린샷 위치**: 채널 관리 페이지의 왼쪽 메뉴

2. **챗봇 활성화**
   - **"챗봇 설정"** 또는 **"자동응답 설정"** 클릭
   - **"챗봇 사용"** 또는 **"자동응답 사용"** 토글을 **ON**으로 변경

   📸 **스크린샷 위치**: 챗봇 설정 페이지의 토글 스위치

---

### 4단계: API 연동 설정 (웹훅 등록)

1. **API 연동 메뉴 접속**
   - 채널 관리 페이지에서 **"API 연동"** 또는 **"웹훅 설정"** 메뉴 클릭
   - 또는 **"챗봇"** → **"API 연동"** 경로로 이동

   📸 **스크린샷 위치**: API 연동 메뉴 위치

2. **웹훅 URL 입력**

   **로컬 개발 (ngrok 사용):**
   ```bash
   # 터미널에서 ngrok 실행
   ngrok http 5000
   ```
   
   ngrok이 제공하는 URL 예시:
   ```
   Forwarding: https://abc123.ngrok.io -> http://localhost:5000
   ```

   **웹훅 URL 입력:**
   - **웹훅 URL** 필드에 입력: `https://abc123.ngrok.io/webhook`
   - 또는 공개 서버 URL: `https://yourdomain.com/webhook`
   - **저장** 또는 **등록** 버튼 클릭

   📸 **스크린샷 위치**: 웹훅 URL 입력 필드

3. **웹훅 테스트** (선택사항)
   - 일부 채널에서는 **"웹훅 테스트"** 버튼 제공
   - 테스트 메시지 전송하여 연결 확인

   📸 **스크린샷 위치**: 웹훅 테스트 버튼 및 결과

---

### 5단계: 채널 연결 확인

1. **채널 연결 상태 확인**
   - API 연동 페이지에서 **"연결 상태"** 또는 **"웹훅 상태"** 확인
   - 정상적으로 연결되면 **"연결됨"** 또는 **"활성화"** 상태 표시

   📸 **스크린샷 위치**: 연결 상태 표시

2. **카카오톡에서 테스트**
   - 카카오톡 앱에서 채널 검색
   - 채널 추가 후 메시지 전송
   - 봇이 응답하는지 확인

   📸 **스크린샷 위치**: 카카오톡 앱에서 채널과의 대화 화면

---

## 웹훅 서버 실행 방법

### 로컬 개발 환경

```bash
# 1. 웹훅 서버 실행
python main.py kakao --webhook

# 2. 다른 터미널에서 ngrok 실행
ngrok http 5000

# 3. ngrok이 제공하는 URL을 카카오 비즈니스에 등록
# 예: https://abc123.ngrok.io/webhook
```

### 서버 배포 환경

**Railway 사용 예시:**
```bash
# Railway CLI 설치
npm i -g @railway/cli

# 로그인 및 배포
railway login
railway init
railway up
```

**Render 사용 예시:**
1. https://render.com/ 접속
2. New Web Service 선택
3. GitHub 저장소 연결
4. Build Command: `pip install -r requirements.txt`
5. Start Command: `python main.py kakao --webhook`
6. Deploy 후 제공되는 URL을 웹훅으로 등록

---

## 웹훅 URL 형식

웹훅 URL은 다음 형식을 따라야 합니다:

```
https://yourdomain.com/webhook
```

또는 ngrok 사용 시:

```
https://abc123.ngrok.io/webhook
```

**중요:**
- HTTPS 필수 (HTTP는 사용 불가)
- `/webhook` 경로로 설정 (코드에서 `/webhook` 엔드포인트 사용)
- 공개적으로 접근 가능한 URL이어야 함

---

## 문제 해결

### 웹훅이 작동하지 않는 경우

1. **웹훅 서버 실행 확인**
   ```bash
   # 웹훅 서버가 실행 중인지 확인
   curl http://localhost:5000/health
   ```

2. **ngrok 연결 확인**
   - ngrok 대시보드에서 요청 로그 확인
   - https://dashboard.ngrok.com/ 접속

3. **카카오 비즈니스 설정 확인**
   - 웹훅 URL이 정확히 입력되었는지 확인
   - 연결 상태가 "연결됨"인지 확인

4. **로그 확인**
   - 웹훅 서버 콘솔에서 요청 로그 확인
   - 오류 메시지 확인

### 자주 발생하는 오류

**오류: "웹훅 URL에 연결할 수 없습니다"**
- 웹훅 서버가 실행 중인지 확인
- ngrok이 실행 중인지 확인
- URL에 `/webhook` 경로가 포함되어 있는지 확인

**오류: "웹훅 응답 형식이 올바르지 않습니다"**
- 웹훅 서버가 올바른 JSON 형식으로 응답하는지 확인
- `webhook_server.py`의 응답 형식 확인

**오류: "메시지가 전달되지 않습니다"**
- 카카오톡 앱에서 채널이 정상적으로 추가되었는지 확인
- 웹훅 서버 로그에서 요청이 들어오는지 확인

---

## 추가 설정 (선택사항)

### 자동응답 메시지 설정

카카오 비즈니스 채널에서 자동응답 메시지를 설정할 수 있습니다:

1. **챗봇 설정** → **"자동응답"** 메뉴
2. **"인사말"**, **"자주 묻는 질문"** 등 설정
3. 웹훅과 함께 사용 가능

### 채널 프로필 설정

1. **채널 관리** → **"프로필"** 메뉴
2. 프로필 이미지, 소개글, 운영 시간 등 설정
3. 사용자에게 보이는 채널 정보 설정

---

## 참고 자료

- **카카오 비즈니스**: https://business.kakao.com/
- **카카오 디벨로퍼스**: https://developers.kakao.com/
- **카카오톡 챗봇 API 문서**: https://developers.kakao.com/docs/latest/ko/kakaotalk-channel/common

---

## 스크린샷 체크리스트

설정 과정에서 다음 스크린샷을 찍어두면 도움이 됩니다:

- [ ] 카카오 비즈니스 로그인 페이지
- [ ] 채널 생성 페이지
- [ ] 챗봇 설정 페이지 (토글 ON 상태)
- [ ] API 연동/웹훅 설정 페이지
- [ ] 웹훅 URL 입력 필드
- [ ] 연결 상태 확인 화면
- [ ] 카카오톡 앱에서 채널 대화 화면
- [ ] 웹훅 서버 콘솔 로그

---

## 완료 확인

다음 항목이 모두 완료되면 설정이 완료된 것입니다:

- [ ] 카카오 비즈니스 채널 생성 완료
- [ ] 챗봇 활성화 완료
- [ ] 웹훅 URL 등록 완료
- [ ] 연결 상태가 "연결됨"으로 표시됨
- [ ] 카카오톡에서 메시지 전송 시 봇이 응답함

설정이 완료되면 이제 완전 무료로 카카오톡 챗봇을 사용할 수 있습니다! 🎉

