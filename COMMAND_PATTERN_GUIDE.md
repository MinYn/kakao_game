# 커맨드 형태 발화 패턴 등록 가이드

카카오 챗봇 관리자센터에서 슬래시(`/`)와 `@` 커맨드를 발화 패턴으로 등록하는 방법을 설명합니다.

---

## 개요

게임봇은 다음 커맨드 형태를 지원합니다:
- **슬래시 커맨드**: `/골드`, `/게임시작 모험` 등
- **@봇이름 커맨드**: `@게임봇 골드`, `@게임봇 게임시작 모험` 등

카카오 챗봇 관리자센터에서 이러한 커맨드를 발화 패턴으로 등록하는 방법을 설명합니다.

---

## 방법 1: 모든 발화로 설정 (권장) ⭐

### 장점

- **가장 간단함**: 발화 패턴을 하나하나 등록할 필요 없음
- **모든 커맨드 자동 지원**: `/골드`, `@게임봇 골드`, `골드` 모두 작동
- **유지보수 쉬움**: 새로운 커맨드 추가 시 발화 패턴 수정 불필요

### 설정 방법

1. **"게임봇 메인 블록" 편집**
2. **"사용자 발화" 섹션**:
   - 발화 패턴 입력 필드: **비워두기**
   - 발화 패턴 목록: **비우기**
   - "모든 발화" 옵션: **활성화** (있는 경우)
3. **저장 및 배포**

**결과**: 
- `/골드`, `@게임봇 골드`, `골드` 모두 작동
- 스킬 서버에서 커맨드 형태를 자동으로 파싱하여 처리

---

## 방법 2: 커맨드 형태 발화 패턴 추가 (선택사항)

특정 커맨드만 이 블록으로 연결하고 싶다면 발화 패턴을 추가할 수 있습니다.

### 슬래시 커맨드 발화 패턴

**"사용자 발화" 섹션에서 발화 패턴 추가**:

1. **발화 패턴 입력 필드**에 발화 입력:
   ```
   /골드
   ```
2. **추가** 버튼 클릭 또는 **Enter** 키

3. **추가 발화 패턴들**:
   ```
   /골드
   /gold
   /g
   /게임시작
   /게임시작 모험
   /게임시작 숫자맞추기
   /게임시작 가위바위보
   /리더보드
   /도움말
   /s a
   /s 모험
   ```

**주의**: 
- 발화 패턴은 **정확히 일치**해야 함
- `/골드`와 `골드`는 다른 발화 패턴
- 모든 슬래시 커맨드를 등록하려면 많은 발화 패턴이 필요함

### @봇이름 커맨드 발화 패턴

**"사용자 발화" 섹션에서 발화 패턴 추가**:

1. **발화 패턴 입력**:
   ```
   @게임봇 골드
   ```
2. **추가** 버튼 클릭

3. **추가 발화 패턴들**:
   ```
   @게임봇 골드
   @게임봇 게임시작 모험
   @게임봇 리더보드
   @게임봇 도움말
   @봇이름 골드
   @봇이름 게임시작 모험
   ```

**주의**: 
- 봇 이름이 다르면 각각 등록해야 함
- `@게임봇 골드`와 `@봇이름 골드`는 다른 발화 패턴
- 봇 이름이 변경되면 발화 패턴도 수정 필요

---

## 방법 3: 와일드카드 패턴 사용 (고급)

카카오 챗봇 관리자센터에서 와일드카드를 지원하는 경우:

### 슬래시 커맨드 와일드카드

**발화 패턴 예시**:
```
/골드
/게임시작 *
/리더보드
/도움말
```

**설명**:
- `/게임시작 *` → `/게임시작`으로 시작하는 모든 메시지
- 예: `/게임시작 모험`, `/게임시작 숫자맞추기` 등

**주의**: 카카오 챗봇 관리자센터에서 와일드카드 지원 여부 확인 필요

### @봇이름 커맨드 와일드카드

**발화 패턴 예시**:
```
@게임봇 *
@봇이름 *
```

**설명**:
- `@게임봇 *` → `@게임봇`으로 시작하는 모든 메시지
- 예: `@게임봇 골드`, `@게임봇 게임시작 모험` 등

**주의**: 와일드카드 지원 여부 확인 필요

---

## 권장 설정

### 게임봇 권장 설정

**방법 1: 모든 발화로 설정** (가장 권장) ⭐

1. **발화 패턴**: 비워두기 (모든 발화)
2. **이유**: 
   - 설정이 간단함
   - 모든 커맨드 형태 자동 지원
   - 스킬 서버에서 파싱 처리

**결과**:
- ✅ `/골드` 작동
- ✅ `@게임봇 골드` 작동
- ✅ `골드` 작동
- ✅ `/게임시작 모험` 작동
- ✅ `@게임봇 게임시작 모험` 작동
- ✅ `게임시작 모험` 작동

### 방법 2: 커맨드 형태 발화 패턴 추가 (선택사항)

**사용 시나리오**:
- 특정 커맨드만 이 블록으로 연결하고 싶을 때
- 다른 블록과 구분하고 싶을 때

**단점**:
- 많은 발화 패턴 등록 필요
- 새로운 커맨드 추가 시 발화 패턴 수정 필요
- 유지보수 복잡

---

## 발화 패턴 예시 목록

### 슬래시 커맨드 발화 패턴

**골드 관련**:
```
/골드
/gold
/g
/포인트
/point
/p
```

**게임 관련**:
```
/게임목록
/게임시작
/게임시작 모험
/게임시작 숫자맞추기
/게임시작 가위바위보
/게임종료
/s a
/s 모험
/s n
/s r
```

**기타**:
```
/리더보드
/도움말
/help
/h
```

### @봇이름 커맨드 발화 패턴

**@게임봇 사용**:
```
@게임봇 골드
@게임봇 게임시작 모험
@게임봇 리더보드
@게임봇 도움말
```

**@봇이름 사용** (봇 이름이 다른 경우):
```
@봇이름 골드
@봇이름 게임시작 모험
```

**주의**: 봇 이름은 실제 챗봇 이름에 맞춰야 함

---

## 실제 설정 예시

### 예시 1: 모든 발화로 설정 (권장)

**블록 설정**:
- 블록 이름: `게임봇 메인 블록`
- 사용자 발화: **비워두기** (모든 발화)
- 파라미터: 설정 안 함
- 응답 설정: 스킬 연결 → "게임봇 스킬"

**결과**: 모든 커맨드 형태 작동

### 예시 2: 슬래시 커맨드만 등록

**블록 설정**:
- 블록 이름: `게임봇 슬래시 커맨드 블록`
- 사용자 발화: `/골드`, `/게임시작 모험`, `/리더보드` 등
- 파라미터: 설정 안 함
- 응답 설정: 스킬 연결 → "게임봇 스킬"

**결과**: 슬래시 커맨드만 작동, 일반 명령어는 다른 블록으로

### 예시 3: @봇이름 커맨드만 등록

**블록 설정**:
- 블록 이름: `게임봇 @커맨드 블록`
- 사용자 발화: `@게임봇 골드`, `@게임봇 게임시작 모험` 등
- 파라미터: 설정 안 함
- 응답 설정: 스킬 연결 → "게임봇 스킬"

**결과**: @봇이름 커맨드만 작동

---

## 주의사항

### 발화 패턴 정확성

**중요**: 발화 패턴은 **정확히 일치**해야 합니다.

- ✅ `/골드` → `/골드` 발화 패턴과 일치
- ❌ `/골드 ` (공백) → `/골드` 발화 패턴과 불일치
- ❌ `골드` → `/골드` 발화 패턴과 불일치

### 봇 이름 확인

**@봇이름 커맨드 사용 시**:
- 실제 챗봇 이름을 확인해야 함
- 챗봇 이름이 "게임봇"이면 `@게임봇` 사용
- 챗봇 이름이 다르면 해당 이름 사용

**확인 방법**:
- 챗봇 관리자센터 → 봇 설정 → 봇 이름 확인
- 또는 카카오톡에서 봇 이름 확인

### 발화 패턴 우선순위

카카오 챗봇 관리자센터는 발화 패턴을 **위에서 아래 순서**로 매칭합니다.

**권장 순서**:
1. 구체적인 발화 패턴 (예: `/게임시작 모험`)
2. 일반적인 발화 패턴 (예: `/게임시작 *`)
3. 기본 블록 (모든 발화)

---

## 문제 해결

### 슬래시 커맨드가 작동하지 않는 경우

1. **발화 패턴 확인**
   - `/골드` 발화 패턴이 정확히 등록되었는지 확인
   - 공백이나 특수문자 확인

2. **"모든 발화" 확인**
   - "모든 발화"가 활성화되어 있으면 모든 커맨드 작동
   - 발화 패턴만 등록되어 있으면 해당 패턴만 작동

3. **스킬 서버 로그 확인**
   - 스킬 서버가 호출되는지 확인
   - 요청 메시지 확인

### @봇이름 커맨드가 작동하지 않는 경우

1. **봇 이름 확인**
   - 실제 챗봇 이름과 일치하는지 확인
   - `@게임봇` vs `@봇이름` 확인

2. **발화 패턴 확인**
   - `@게임봇 골드` 발화 패턴이 정확히 등록되었는지 확인
   - 공백 확인

3. **스킬 서버 로그 확인**
   - 요청 메시지에서 `@게임봇` 부분이 제거되는지 확인

---

## 요약

### 게임봇 권장 설정

✅ **"모든 발화"로 설정**
- 발화 패턴: 비워두기
- 모든 커맨드 형태 자동 지원
- 설정이 간단하고 유지보수 쉬움

### 커맨드 형태 지원

**슬래시 커맨드**:
- `/골드`, `/게임시작 모험` 등
- 스킬 서버에서 자동 파싱

**@봇이름 커맨드**:
- `@게임봇 골드`, `@게임봇 게임시작 모험` 등
- 스킬 서버에서 자동 파싱

**기존 명령어**:
- `골드`, `게임시작 모험` 등
- 계속 작동

**결론**: "모든 발화"로 설정하면 모든 커맨드 형태가 자동으로 작동합니다! 🎉

